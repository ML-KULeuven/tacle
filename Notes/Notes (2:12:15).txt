# Excel Constraints
- Sum over rows and columns (or average / max / min)
- All different => enum type?
- Series (ordered)
- Permutations (subsumes series) over numbers or elements
- Rank / position
- Exact / Approximate Lookup (across table)
- Enum mapping
- Conditional sum / ...
- (Previous?) = expanded row

- Mapping / In Set / Lookup - related?